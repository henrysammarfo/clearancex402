# Hackathon feedback — MetaMask × 1Shot × Venice Cook Off

## MetaMask Smart Accounts Kit

- Document a minimal Base Sepolia end-to-end sample: permission grant → x402 pay → revoke in one repo.
- Clarify when `@metamask/delegation-toolkit` vs `@metamask/smart-accounts-kit` should be used (deprecation notice was easy to miss).

## x402 on Base Sepolia

- Venice Vision x402 endpoint works well; a cook-off checklist (Circle USDC faucet + `WALLET_PRIVATE_KEY` probe wallet) would save setup time.
- x402.org facilitator docs are clear; linking from HackQuest resources would help.

## Venice AI

- OpenAI-compatible eval API is straightforward; document recommended model ids for structured JSON eval responses.

## 1Shot

- Prize rules require **mainnet** relayer — consider a testnet-only alternate track or clearer callout for builders without mainnet budget.

## HackQuest

- Workshop recording links in the Resources tab would help late joiners (submission Jun 15).
